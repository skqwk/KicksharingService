spring:
  sql:
    init:
      mode: never
  datasource:
    password: sa
    url: "jdbc:h2:./test_db;DB_CLOSE_ON_EXIT=TRUE"
    username: test
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: update
    generate-ddl: true
  mail:
    properties:
      mail:
        smtp:
          starttls:
            enable: 'true'
          ssl:
            enable: 'true'
          auth: 'true'
    host: smtp.yandex.ru
    username: spring-server@yandex.ru
    password: y/%-q%,Rz[#SV9J
    port: '465'

jwt:
  secretKey: "test-kicksharingmegaverysupersecretkey"
  tokenPrefix: "test-Bearer"
  tokenExpirationAfterDays: 14

manager-token: "test-manager_token"
